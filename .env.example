# Copia este archivo a .env y completa los valores
DATABASE_URL=postgres://<user>:<password>@<host>/<db>?sslmode=require
JWT_SECRET=<genera_un_secreto_seguro>
PUBLIC_BASE_URL=https://tu-dominio.com

# Email (OBLIGATORIO para producción - sin esto no se envían emails)
RESEND_API_KEY=re_xxxxxxxxx  # Obtener de https://resend.com/api-keys
MAIL_FROM="ZARO Certificados <no-reply@tu-dominio.com>"  # Debe coincidir con dominio verificado
APP_NAME=ZARO Certificados
PASSWORD_RESET_TTL_MIN=30

# Variables de entorno en Netlify:
# 1. Ve a tu panel de Netlify > Site settings > Environment variables
# 2. Añade todas las variables arriba (especialmente RESEND_API_KEY y MAIL_FROM)
# 3. Redeploy el sitio para que tomen efecto
